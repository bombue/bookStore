input {
  file {
    path => "/logs/ws-user.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    codec => "json"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "user-logs-%{+YYYY.MM.dd}"
  }
}