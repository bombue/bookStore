input {
  file {
    path => "/logs/ws-author.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    codec => "json"
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "author-logs-%{+YYYY.MM.dd}"
  }
}